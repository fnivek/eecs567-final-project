#include "helpers.h"

Point2 CastPoint2(float x, float y) {
	Point2 newPoint;
	newPoint.x = x;
	newPoint.y = y;

	return newPoint;
}

Point3 CastPoint3(float x, float y, float z) {
	Point3 newPoint;
	newPoint.x = x;
	newPoint.y = y;
	newPoint.z = z;

	return newPoint;
}

