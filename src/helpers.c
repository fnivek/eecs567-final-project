#include "helpers.h"

Point2 CastPoint2(double x, double y) {
	Point2 newPoint;
	newPoint.x = x;
	newPoint.y = y;

	return newPoint;
}

Point3 CastPoint3(double x, double y, double z) {
	Point3 newPoint;
	newPoint.x = x;
	newPoint.y = y;
	newPoint.z = z;

	return newPoint;
}

